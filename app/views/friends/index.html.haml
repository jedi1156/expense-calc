%h2
	Your friends
%table
	- user_friends.each do |f|
		%tr
			%td= f
			%td= link_to "Unfriend", friend_path(f), method: :delete, data: { confirm: "Are you insane!? Think twice before doing that!" }, class: "btn--destroy"
= link_to "See friend requests", friend_requests_path, class: "btn--edit"
