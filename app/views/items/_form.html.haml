= simple_form_for [reckoning, item] do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :description, as: :text, input_html: { class: "span6", rows: 10 }
    = f.input :bought_at, as: :datetime

  .form-actions
    = f.button :submit
