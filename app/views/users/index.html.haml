%h3 Users
%ul.users
	- (@users - [current_user]).each do |user|
		%li
			= link_to user.name, user
			signed up #{user.created_at.to_date}
			- unless current_user.friends.include? user
				= link_to "Request friends", friend_requests_path(friend_id: user.id), method: :post
